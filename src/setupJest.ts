import 'jest-preset-angular';
import 'jest-zone-patch';
import './jestGlobalMocks'; // browser mocks globally available for every test

// Enable this line to decrease, the code that returns jest when finding errors in the tests.
// Remember that the implementation of jest is not "perfect".

//Error.stackTraceLimit = 1;